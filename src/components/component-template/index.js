// import a CSS module
import React, { h, Component } from 'react'
import './index.css'

export default class ComponentTemplate extends Component {
  state = {
  };

  componentDidMount () {
  }

  render () {
    return()
  }
}
